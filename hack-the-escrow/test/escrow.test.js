const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Escrow", function () {
  let releaser, user, reciever;

  beforeEach(async () => {});

  describe("User Deposit", () => {
    it("should depositFor() using payer address", async function () {});
    it("should withdraw() using releaser", async function () {});
    it("should not withdraw() if not releaser", async function () {});
  });
});
